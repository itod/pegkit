@before {
    PKTokenizer *t = self.tokenizer;
    [t setTokenizerState:t.symbolState from:';' to:';'];
}

sections = section+;
section = header? keyVal+;
header = '[' Word+ ']'! ';'!;
keyVal = key '='! val+ ';'!;
key = { return NE(LS(1), @";"); }? Any;
val = { return NE(LS(1), @";"); }? Any;